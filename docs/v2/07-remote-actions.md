# Remote actions (superseded)

> **Superseded by [23-remote-functions.md](./23-remote-functions.md).** The public `action()` factory has been removed in v2.

Use `query`, `mutation`, and `form` in `*.remote.ts` modules instead. See the migration table in [BREAKING-CHANGES.md](./BREAKING-CHANGES.md#remote-functions).

Historical implementation notes for the old unified `action()` API lived here; they are no longer accurate for current Kiru.
