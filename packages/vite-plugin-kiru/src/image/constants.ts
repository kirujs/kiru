/**
 * Tiny LQIP for `placeholder="blur"` (see docs/router/kiru-image.md).
 * Intentionally small — not a responsive variant width.
 */
export const BLUR_PLACEHOLDER_MAX_WIDTH = 8

/** JPEG quality for blur data URLs (small file, heavy blur in CSS). */
export const BLUR_PLACEHOLDER_JPEG_QUALITY = 40
