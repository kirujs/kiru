/** Signed context token on JSON RPC requests and action responses. */
export const KIRU_TOKEN_RESPONSE_HEADER = "x-kiru-token" as const

/** Comma-separated route ids to refetch after a successful action. */
export const KIRU_INVALIDATE_RESPONSE_HEADER = "x-kiru-invalidate" as const
