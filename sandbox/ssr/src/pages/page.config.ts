import type { RoutePageConfig } from "kiru/router"

export default {
  head: {
    title: "Home — Threadboard",
    description: "Hot posts from communities across Threadboard.",
  },
} satisfies RoutePageConfig
