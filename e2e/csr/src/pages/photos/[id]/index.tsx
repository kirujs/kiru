export default function PhotoDetailPage() {
  return () => (
    <main data-testid="photo-detail-page">
      <h2>Photo detail (full page)</h2>
      <p data-testid="photo-detail-body">Rendered as a full route.</p>
    </main>
  )
}
