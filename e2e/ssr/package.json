{
  "name": "e2e-ssr",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development vite",
    "build": "vite build",
    "server:e2e": "cross-env NODE_ENV=production PORT=5192 node dist/server",
    "cy:run": "cypress run",
    "test": "pnpm run build && node ./scripts/verify-hybrid-prerender.mjs && node ./scripts/concurrent-request-context.mjs && pnpm exec cypress run && pnpm exec cypress run --config-file cypress.tier3.config.ts"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.17.0",
    "cross-env": "^10.1.0",
    "cypress": "^13.17.0",
    "vite": "^7.3.1",
    "vite-plugin-kiru": "workspace:^"
  },
  "dependencies": {
    "@kirujs/adapter-node": "workspace:^",
    "kiru": "workspace:^",
    "sharp": "^0.34.5"
  }
}
