import type { RouteScopeConfig } from "kiru/router"

export default {
  head: {
    title: "Threadboard",
    description: "E2E Threadboard replica (sandbox SSR home pattern).",
  },
} satisfies RouteScopeConfig
