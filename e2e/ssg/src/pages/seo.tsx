export default function SeoPage() {
  return (
    <div>
      <p data-testid="ssg-seo">SSG SEO demo page</p>
      <p data-testid="ssg-seo-hint">
        Check document head for application/ld+json and build output for
        sitemap.xml.
      </p>
    </div>
  )
}
